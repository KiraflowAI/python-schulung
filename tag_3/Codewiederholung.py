# zahl = 102
# while zahl < 100:
#     print(zahl)
#     zahl = zahl * 2
# print (f'Finaler Wert: {zahl}.')

#Die Variable zahl wird mit dem Wert 1 angelegt.
# Es wird geprüft, ob der Wert in zahl kleiner als 100 ist. Wenn ja, werden die nächsten beiden Zeilen ausgeführt. Wenn nein, so wird direkt zu Zeile 5 gesprungen.
# Auf der Konsole wird der aktuelle Wert von zahl ausgegeben.
# Der Wert von zahl wird verdoppelt und in zahl gespeichert. Achtung: Danach geht es in Zeile 2 weiter!
# Abschließend wird der finale Wert von zahl ausgegeben.

#Solltest Du in eine Endlosschleife geraten, kannst Du ins Terminal klicken und die Ausführung mit strg + c abbrechen.

#Wenn die Bedinungung nach dem while nie zu False auswertet, dann wird die Schleife für immer ausgeführt

# a = 10
# while a > 0:
#     print(a)
#     a = a - 3

#Die Schleiferumpf wird insgesamt drei Mal durchlaufen.Die Bedingung wird vier Mal geprüft.Auf der Konsole erscheint


#a hat am Ende des Programms den Wert -2. Wenn man die Zeilen 3 und 4 vertauscht ändert sich nur die Konsolenausgabe. Alles andere bleibt gleich

a = 10
while a > 0:
    a = a - 3
    print(a)